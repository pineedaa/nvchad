---@type ChadrcConfig
local M = {}

M.ui = {
  theme = 'rxyhn',
  statusline = {
    theme = 'default',
    separator_style = 'round'
  }
}

-- M.plugins = "plugins"
-- M.mappings = require "mappings"

return M
